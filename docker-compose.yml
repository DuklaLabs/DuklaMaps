services:
  duklamaps:
    build: .
    ports:
      - "5000:5000"
    volumes:
      # Persistentní data mimo kontejner
      - ./timetableData:/app/timetableData
      - ./logs:/app/logs
    restart: unless-stopped
